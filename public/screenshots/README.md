# Screenshots

Drop the three screenshots here before publishing the website.

| Filename       | Content                                         |
|----------------|-------------------------------------------------|
| `hero.png`     | Play tab — full window (installations sidebar + Play button) |
| `mods.png`     | Mods tab — full window (mod list with toggles)  |
| `profile.png`  | Profile tab — full window (profile view)        |

Recommended: export at 2× resolution (2560×1600 or similar) for sharp display on high-DPI screens.
The `<img>` tags in `jojoclient.html` reference these files by name.
